# apps/web

The legacy frontend remains in the repository root during PR-01.

`apps/web` will be introduced in a later migration PR after the v2 backend
surfaces are ready. This placeholder exists to document the intended monorepo
target without moving the current Vite app yet.
